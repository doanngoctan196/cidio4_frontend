var obj = {
    domain: 'http://localhost:8000'
}
    


var StateInfoSystem = (state = obj, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

module.exports = StateInfoSystem;